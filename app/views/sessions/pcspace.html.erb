<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
<style> 
   
</style>
</head>
<body>
  <h1> The World Of PC Gamers </h2>
  <h1>***********************************************</h1>
  <p>Get details on the best games on PC, right here, right now.</p>
  <h1>-----------------------------------------</h1>
  <div class="flex-parent jc-center">
  <% if session.has_key?(:user_id) %>
      <%= link_to '<button type="button">Add Game</button>'.html_safe, new_game_path %>
      <%= link_to '<button type="button">Game List</button>'.html_safe, games_path %>
      <%= link_to '<button type="button">Sign Out</button>'.html_safe, signout_path, method: :delete %>
  <% end %>
  </div>

  <div>
    <% if flash[:message] %>
      <%= flash[:message] %>
    <% end %>
  </div>
</body>
</html>